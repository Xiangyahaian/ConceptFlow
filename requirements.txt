# Python >= 3.10
manim>=0.18.0,<0.22
numpy>=1.24

# System (install yourself, not via pip):
#   - FFmpeg: https://ffmpeg.org/download.html  (ffmpeg + ffprobe on PATH)
#   - Chinese font: Microsoft YaHei (Windows) / PingFang SC (macOS)
